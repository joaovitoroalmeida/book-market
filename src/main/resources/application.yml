server:
  port: 8081

spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://localhost:3306/bookmarket?createDatabaseIfNotExist=true
    username: root
    password: bookmarketKT
